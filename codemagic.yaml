workflows:
  phone-dialer-debug:
    name: Phone Dialer Debug APK

    environment:
      java: 17

    scripts:
      - name: Build Debug APK
        script: |
          chmod +x gradlew
          ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/*.apk
